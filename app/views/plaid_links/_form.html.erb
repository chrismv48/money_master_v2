<%= form_for(plaid_link) do |f| %>
  <% if plaid_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plaid_link.errors.count, "error") %> prohibited this plaid_link from being saved:</h2>

      <ul>
      <% plaid_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
